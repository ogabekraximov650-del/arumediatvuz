name = "arumediatv"
main = "build/worker/shim.mjs"
compatibility_date = "2024-01-01"
workers_dev = true

[build]
command = "cargo install -q worker-build && worker-build --release"

[env.production]
name = "arumediatv"
workers_dev = true

# Secrets (GitHub Actions orqali o'rnatiladi):
# wrangler secret put TURSO_URL --env production
# wrangler secret put TURSO_TOKEN --env production
# wrangler secret put B2_KEY_ID --env production
# wrangler secret put B2_APPLICATION_KEY --env production
# wrangler secret put TELEGRAM_BOT_TOKEN --env production
# wrangler secret put TEZCHEK_API_KEY --env production
#
# TEZCHEK_API_KEY — tezchek.uz kassasining API kaliti. U FAQAT
# worker ichida ishlatiladi va ilovaga hech qachon chiqmaydi.
